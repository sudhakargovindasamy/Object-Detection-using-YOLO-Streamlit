# Frontend (Vercel) - Public variables (NEXT_PUBLIC_ prefix)
# Deploy backend first, then add the URL here
NEXT_PUBLIC_API_URL=

# Backend - Private variables (not exposed to frontend)
# No private secrets needed for this application
# YOLO models are downloaded automatically from Ultralytics

# Optional: Custom model URL (if using a private model repository)
# MODEL_URL=